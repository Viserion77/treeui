# TreeUI for Claude Code

Follow [AGENTS.md](/workspaces/treeui/AGENTS.md) as the primary repo guidance.

Additional Claude Code note:

- If `.mcp.json` is present and dependencies are installed, the local `treeui` MCP server can be used to search components, retrieve setup guidance, and recommend the right component for a given task.

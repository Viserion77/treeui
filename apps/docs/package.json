{
  "name": "@treeui/docs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docs:dev": "pnpm exec storybook dev -p 6006 --host 0.0.0.0 --ci",
    "docs:build": "pnpm exec storybook build",
    "typecheck": "pnpm exec vue-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@treeui/icons": "workspace:*",
    "@treeui/tokens": "workspace:*",
    "@treeui/vue": "workspace:*",
    "vue": "^3.5.13"
  }
}

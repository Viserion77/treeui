{
  "name": "@treeui/landing",
  "version": "0.1.9",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm exec vite",
    "build": "pnpm exec vite build",
    "typecheck": "pnpm exec vue-tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@treeui/tokens": "workspace:*",
    "@treeui/vue": "workspace:*",
    "vue": "^3.5.40"
  }
}

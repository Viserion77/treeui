version: 0.1
package: "@treeui/vue"
public_component_prefix: "T"
legacy_component_prefix: "Tree"
legacy_alias_status: "compatibility_only"
source_component_filename_prefix: "Tree"

primary_exports:
  - TAlert
  - TBadge
  - TButton
  - TCard
  - TCheckbox
  - TDatePicker
  - TFormField
  - TInput
  - TModal
  - TRadio
  - TRadioGroup
  - TSelect
  - TSkeleton
  - TSpinner
  - TSwitch
  - TToast
  - TToastProvider
  - TTooltip

compatibility_aliases:
  TAlert: TreeAlert
  TBadge: TreeBadge
  TButton: TreeButton
  TCard: TreeCard
  TCheckbox: TreeCheckbox
  TDatePicker: TreeDatePicker
  TFormField: TreeFormField
  TInput: TreeInput
  TModal: TreeModal
  TRadio: TreeRadio
  TRadioGroup: TreeRadioGroup
  TSelect: TreeSelect
  TSkeleton: TreeSkeleton
  TSpinner: TreeSpinner
  TSwitch: TreeSwitch
  TToast: TreeToast
  TToastProvider: TreeToastProvider
  TTooltip: TreeTooltip

shared_sizes:
  values: [sm, md, lg]
  used_by:
    - TBadge
    - TButton
    - TCard
    - TDatePicker
    - TFormField
    - TInput
    - TSelect
    - TTextarea
    - TModal
    - TSpinner
    - TSwitch

shared_variants:
  action_values: [solid, outline, ghost, soft, danger]
  card_values: [outline, soft, solid]

shared_props:
  variant:
    used_by: [TBadge, TButton, TCard]
  size:
    used_by: [TBadge, TButton, TCard, TCheckbox, TDatePicker, TInput, TModal, TRadio, TRadioGroup, TSelect, TSpinner, TTextarea]
  disabled:
    used_by: [TButton, TCheckbox, TDatePicker, TInput, TModal, TRadio, TRadioGroup, TSelect, TSwitch, TTextarea, TTooltip]
  loading:
    used_by: [TButton, TInput, TSelect, TTextarea]
  as:
    used_by: [TButton, TCard]
  class:
    status: "supported via standard Vue attrs when relevant"

shared_events:
  update:modelValue:
    used_by: [TCheckbox, TDatePicker, TInput, TRadioGroup, TSelect, TSwitch, TTextarea]
  update:open:
    used_by: [TDatePicker, TModal, TSelect, TTooltip]
  open-change:
    used_by: [TDatePicker, TModal, TSelect, TTooltip]

shared_slots:
  default:
    used_by: [TBadge, TButton, TCard, TInput, TModal, TTooltip]
  icon:
    used_by: [TBadge, TButton]
  prefix:
    used_by: [TDatePicker, TInput]
  suffix:
    used_by: [TDatePicker, TInput]
  trigger:
    used_by: [TModal, TTooltip]
  content:
    used_by: [TModal, TTooltip]
  header:
    used_by: [TCard, TModal]
  footer:
    used_by: [TCard, TModal]

value_formats:
  date: "YYYY-MM-DD"

overlay_contract:
  props:
    - open
    - defaultOpen
  events:
    - update:open
    - open-change
  rule: "controlled and uncontrolled modes should behave the same from the outside"

form_contract:
  props:
    - modelValue
  events:
    - update:modelValue

accessibility_baseline:
  focus_visible: true
  keyboard_support: true
  semantic_markup: true
  disabled_state_clarity: true
  loading_state_clarity: true
  screen_reader_labels_when_needed: true

notes:
  - "Prefer T<Name> in examples, docs, and new code."
  - "Keep Tree<Name> only for migration compatibility."
  - "Do not introduce Vue-specific naming into tokens or design contracts."

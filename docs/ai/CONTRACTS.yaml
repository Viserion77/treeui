version: 0.1
package: "@treeui/vue"
public_component_prefix: "T"
source_component_filename_prefix: "T"

# Every component value exported from packages/vue/src/components/index.ts.
# This list is the canonical answer to "does TreeUI already have X?" — check it
# before proposing a new component or a new prop, because a capability that
# already ships here is not missing, only undocumented.
# Maintenance: must stay 1:1 with the `export { default as ... }` lines in
# packages/vue/src/components/index.ts. Verify with:
#   grep -oP '^export \{ default as \K\w+' packages/vue/src/components/index.ts | sort
primary_exports_count: 96
primary_exports:
  - TAccordion
  - TAccordionItem
  - TAlert
  - TAppBar
  - TAppShell
  - TAvatar
  - TBadge
  - TBrandLockup
  - TBreadcrumb
  - TBreadcrumbItem
  - TButton
  - TCard
  - TChart
  - TCheckbox
  - TCodeBlock
  - TColorSwatch
  - TCombobox
  - TConfirmDialog
  - TContainer
  - TContextMenu
  - TDatePicker
  - TDateTimePicker
  - TDescriptionItem
  - TDescriptionList
  - TDivider
  - TDonutChart
  - TDrawer
  - TDropdown
  - TEmptyState
  - TFileUpload
  - TFlag
  - TFormField
  - TGrid
  - THero
  - THide
  - TIcon
  - TImage
  - TInput
  - TKbd
  - TKeyValueEditor
  - TLanguageSelect
  - TLink
  - TLinkTile
  - TList
  - TListItem
  - TMarkdownEditor
  - TMenu
  - TMenuGroup
  - TMenuItem
  - TModal
  - TMultiSelect
  - TNavMenu
  - TNavbar
  - TNumberInput
  - TPage
  - TPageHeader
  - TPageSurface
  - TPagination
  - TPopover
  - TPricing
  - TPricingCard
  - TProgress
  - TRadio
  - TRadioGroup
  - TSection
  - TSelect
  - TSelectableList
  - TShow
  - TSidebar
  - TSkeleton
  - TSkipLink
  - TSpacer
  - TSparkline
  - TSpinner
  - TSplit
  - TStack
  - TStackItem
  - TStat
  - TStepper
  - TSteps
  - TSwitch
  - TTab
  - TTabList
  - TTabPanel
  - TTable
  - TTabs
  - TTag
  - TTagInput
  - TText
  - TTextarea
  - TTimeline
  - TToast
  - TToastProvider
  - TToggleGroup
  - TTooltip
  - TTreeView

manifest_status:
  values: [available]
  note: >-
    The `status:` field of every COMPONENTS/*.yaml manifest, surfaced verbatim in
    the generated catalog and by the MCP server. Everything in primary_exports is
    shipped and plugin-registered, so `available` is currently the only value.
    Add a new value here first if a manifest ever needs one — do not invent
    per-file labels such as `draft` or a milestone name.

export_aliases:
  note: "Two exports are aliases of the same component, not separate components. Counted individually above."
  pairs:
    - canonical: TNavbar
      alias: TAppBar
    - canonical: TSteps
      alias: TStepper

global_component_typing:
  note: >-
    `@treeui/vue` augments Vue's `GlobalComponents` with exactly what
    `TreeUIPlugin` registers (the augmentation IS `typeof components` from
    plugin.ts, so it cannot drift). Without it `vue-tsc` has nothing to check a
    globally registered `<TTag>` against, and a prop that does not exist is not
    an error: it becomes an entry in `$attrs` and, on a component with
    `inheritAttrs: false`, lands on an inner element as an invalid HTML
    attribute and disappears. One consumer audit found six such props across 19
    call sites — a checkbox with no accessible name, a destructive button in the
    default colour, translated copy that never rendered — with no warning in
    dev, in build, or in `vue-tsc`.
  trade_off: >-
    The augmentation is unconditional, so an app importing components by name
    instead of installing the plugin is also told they resolve globally. That
    direction fails loudly at runtime ("Failed to resolve component"); the
    direction it fixes failed silently.

layout_capabilities_already_shipping:
  note: >
    Layout sizing and flex behavior already exist as dedicated primitives. Reach
    for these before adding `fill`, `grow`, `flex`, or width props to individual
    components — that capability is composed, not duplicated per component.
  TStackItem:
    props: [grow, shrink, basis, minWidth, align]
    purpose: >
      Per-child flex control inside a TStack. `grow` makes one child absorb the
      free space; `minWidth` is the standard fix for a flex child that refuses to
      shrink below its content.
  TStack:
    purpose: "One-dimensional layout — direction, gap, align, justify, wrap."
  TSplit:
    purpose: "Two-region row that pushes the second region to the far edge."
  TSpacer:
    purpose: "Flexible gap that absorbs free space between siblings."
  TGrid:
    purpose: "Responsive multi-column layout."
  TContainer:
    purpose: "Max-width constraint and horizontal page padding."
  TPage:
    purpose: "Page region: width constraint plus block padding and vertical rhythm."
  TSection:
    purpose: >
      Marketing/page section: responsive vertical rhythm plus an optional
      full-bleed band, with the reading column delegated to a nested TContainer.
      Use instead of wrapping a TContainer in a hand-written `<div class="banded">`.
  THero:
    purpose: >
      The one section with a layer behind the copy: stacking context, clip,
      hero-scale padding, and a non-interactive `#backdrop` slot.
  TPageSurface:
    purpose: >
      Page-level surface for screens with NO app shell (landing, login, public
      status). Zeroes the user-agent body margin and, with `overlay`, takes the
      viewport and becomes the scroll host.
  TShow_THide:
    purpose: >
      Breakpoint visibility in pure CSS — both branches render, so a
      pre-rendered page keeps every link. Reach for these before a `matchMedia`
      composable; a media query cannot read `--tree-breakpoint-*`.

shared_sizes:
  values: [sm, md, lg]
  note: >-
    The shared scale for interactive and form components. A few components carry
    their own `size` scale for a different axis (TContainer, TText, TIcon,
    TDonutChart); each is declared in its own COMPONENTS/*.yaml.

shared_variants:
  action_values: [solid, outline, ghost, soft, danger]
  card_values: [outline, soft, inset]
  card_values_note: >
    Card variants are a surface scale (plain → tinted → recessed), a different
    axis from the action variants above. `solid` is deliberately absent: it is
    an action variant only. See DECISIONS.md → "Variant Vocabulary".
  button_extra_values:
    values: [brand]
    note: "TButton adds a high-emphasis `brand` gradient variant (via `--tree-gradient-brand`) on top of the shared action values. It is button-only, not a shared action variant."

shared_props:
  # Per-component support is declared in COMPONENTS/*.yaml — that is the single
  # place a prop roster is kept true. The entries below define the shared
  # contract (values, defaults, semantics), not the list of components using it.
  variant:
    values_note: "See shared_variants — action, card, and the button-only `brand` value."
  tone:
    note: >-
      Status tone, not an action variant. The accepted values differ per
      component (TBadge/TStat omit `brand`, TTimeline omits `info`, TTag adds
      `accent`, and TText's `tone` is a text-color role — default | muted |
      inverse | brand — EXTENDED with the four status tones, since a sentence
      that is the state is neither an alert box nor a pill).
      See DECISIONS.md → "Variant Vocabulary".
    not_universal: >-
      `tone` is NOT on every component with a destructive state: TButton and
      TLink express it through `variant` (`danger` is a member of the shared
      action scale) and TConfirmDialog through `confirmVariant`. Reaching for
      `tone` on those is the single most common wrong guess; `GlobalComponents`
      typing (see `global_component_typing`) now turns it into a compile error
      instead of a silent no-op.
  accent:
    type: TAccent
    values: [brand, neutral, success, warning, danger, info]
    default: "unset — the secondary brand accent from @treeui/tokens"
    used_by: [TSection, THero, TPageSurface]
    note: >-
      The surface accent axis: a closed set a surface declares once and its
      whole subtree inherits through `--tree-color-accent-primary` /
      `-hover` / `-soft` / `-contrast`. Anything that reads those follows —
      `TTag tone="accent"`, the `THero glow` halo, `--tree-shadow-accent`.
      Never a free colour: a product picks a tone, not a hex, which is the same
      policy TLinkTile's `tone` states. Left unset, the accent is the secondary
      brand accent the tokens ship (a measured light/dark pair, AA on every
      background and on its own soft tint).
  size:
    values: [sm, md, lg]
  width:
    values: [xs, sm, md, lg, xl, full]
    default: full
    note: >-
      Inline-size scale for form controls (type `TFieldWidth`). Controls are
      fluid by default; the other steps set a `max-inline-size` cap so they
      still shrink on narrow containers. Prefer this over inline width styles.
      Caps: xs 8rem, sm 12rem, md 18rem, lg 24rem, xl 32rem.
    other_width_contracts: >-
      `width` is only this scale on form controls. Overlay panels (TPopover,
      TMenu) take `TPopoverWidth` (content | sm | md | lg), TPage takes the
      container max-width scale `TContainerSize`, and TSidebar/TSkeleton take a
      raw CSS length while TSparkline takes a number of pixels. Check the
      component manifest before assuming `full` is valid.
  disabled:
    note: "Non-native disabled elements must also set `aria-disabled`."
  loading:
    note: >-
      loading conveys busy state. On single action controls (TButton, TConfirmDialog)
      it also disables that one control to prevent double submission. On form value
      controls (TFileUpload, TInput, TSelect, TTextarea, TCombobox, TLanguageSelect) it is
      presentational only and must never gate interaction.
  as:
    note: >-
      Polymorphic root element (a tag name). Carried by the layout primitives
      and by TButton/TCard; the manifest of each says which tags make sense.
    ua_boxes: >-
      Swapping the element brings the browser's own box with it, and the library
      cancels the two that bite: `as="ul"/"ol"/"menu"` on TStack/TGrid/TSplit/
      TContainer drops the marker, indent and margin (and restores `role="list"`,
      which `list-style: none` removes in Safari), and `as="button"` on TCard
      restores font, width and text alignment, which a `<button>` does not
      inherit. Do NOT re-write those resets in product CSS.
  family:
    values: [sans, mono]
    used_by: [TText, TLink, TTextarea]
    note: >-
      Font family, one vocabulary across the text surfaces. `mono` is for
      machine text — ids, ARNs, keys, JSON — and is what replaces a local
      `.mono` class.
  wrap:
    values: [anywhere, break-word]
    used_by: [TText]
    note: >-
      Break axis for a string with no break opportunity. Distinct from
      `truncate`, which is the wrong answer when the string IS the datum the
      reader came to copy. Mutually exclusive with it.
  label:
    note: >-
      `label` is the component's own label — visible where the component paints
      one (TStat, TDescriptionItem, TFormField), announced-only where it has no
      visible text (TIcon, TSpinner, TProgress, TPagination, an `iconOnly`
      TButton). Either way it is the single name of the component.
  ariaLabel:
    note: >-
      `ariaLabel` is an accessible SUMMARY layered over content that is already
      visible — the dataviz trio (TChart, TSparkline, TDonutChart), where the
      picture is on screen but needs a sentence for screen readers. Use `label`
      for anything that is the component's own name; new components should not
      add `ariaLabel` for that job.
  icon:
    type: TIconInput
    type_definition: >-
      `TIconInput = TIconName | Component` — a registered icon name, or a
      component supplied by the application. Exported from `@treeui/icons` and
      re-exported from `@treeui/vue`.
    used_by: [TNavMenu]
    used_by_note: >-
      As the `icon` field of a `TNavMenuItem` — and that is the ONLY prop or item
      field in the library that takes an icon as a value. Components that show an
      icon elsewhere (TAlert, TBadge, TButton, TConfirmDialog, TEmptyState,
      TFileUpload, TStat, TTag) expose an `#icon` SLOT and have no `icon` prop;
      `TIcon` takes `name: TIconName`, a registry name only, never a component.
      The shared rule is therefore about typing, not reach: where a value-shaped
      icon exists it is `TIconInput`, never `Component` alone. A name needs no
      import, and — being a string — is never made reactive, so it survives a
      `ref`/`reactive` item list, an API response, or a route definition without
      `markRaw`.
    resolution:
      - >-
        Names are resolved centrally, never per component: `resolveTreeIcon(icon)`
        maps a name OR a component to a component. A template must call it before
        `<component :is>`, because Vue resolves a raw string `is` as a globally
        registered component name — passing `"cpu"` straight through renders
        nothing.
      - >-
        `TIcon` is the render path for a standalone icon. Its `name` prop is a
        `TIconName` (registry names only); when the app holds a component rather
        than a name, pass it to an `icon` prop or render it in a slot.
      - >-
        An unknown name resolves to `undefined` and logs a warning listing the
        valid names — once per name, and in production builds as well as dev.
        Consumers degrade rather than break — `TNavMenu` falls back to its letter
        marker.
      - >-
        Components are built lazily on first lookup and cached, so an icon nobody
        renders stays as geometry instead of becoming a component. That is a
        construction saving, not a bundle one: the built-in geometry is held in a
        module-scope `Map`, so it is retained by any import of the package.
    registry:
      - >-
        Names come from `TIconRegistry`, an augmentable interface;
        `TIconName = keyof TIconRegistry & string`. Apps widen it with
        `declare module '@treeui/icons'` — see SETUP.yaml → `icons`.
      - >-
        `registerTreeIcons(icons: Record<string, TIconDefinition>)` adds icons,
        where `TIconDefinition = TIconNodes | Component`. `hasTreeIcon(name)`
        tests one, `listTreeIcons()` returns all sorted names, `treeIcons` is a
        lazy name → component map, and `builtinTreeIconNodes` is the shipped
        GEOMETRY by name (`Record<string, TIconNodes>` — data, not components).
      - >-
        Registering one of the 364 built-in names replaces that built-in for the
        whole process and does not warn. Additions must use a name TreeUI does
        not ship; replacement is supported but has to be intentional.
      - >-
        `resetTreeIcons()` restores the registry to what TreeUI ships and drops
        everything registered since. The registry is process-wide mutable state
        (anchored on a `Symbol.for` global so duplicate package copies share it),
        so tests that register icons must call it in an `afterEach`.
      - >-
        Individual icons are NOT exported as named imports. The registry is the
        public surface; there is no `import { Cpu } from '@treeui/icons'`.
      - >-
        Author custom geometry with `createTreeIcon(name, nodes)`, never a
        hand-rolled `<svg>`. Icons draw on a 24-unit viewBox at a pixel `size`,
        so painted stroke = attribute × size / 24. `absoluteStrokeWidth` sets the
        attribute to `(strokeWidth * 24) / size`, cancelling the scale and
        painting a constant 2px at every size; a hand-rolled `stroke-width="2"`
        paints 1.67px at 20, 2.67px at 32 and 4px at 48, so it grows heavier as
        the icon grows. `createTreeIcon` also supplies the `size` / `strokeWidth`
        props and the `aria-hidden` default.
  class:
    status: "supported via standard Vue attrs when relevant"

shared_events:
  # As with shared_props: which components emit these is declared in
  # COMPONENTS/*.yaml. Here is what they mean.
  update:modelValue:
    note: "Emitted by every component with a `modelValue` prop — see form_contract."
  update:open:
    note: "Emitted by every overlay component — see overlay_contract."
  open-change:
    note: "Emitted alongside `update:open` by every overlay component — see overlay_contract."

shared_slots:
  default:
    used_by: [TBadge, TButton, TCard, TConfirmDialog, TFileUpload, TInput, TModal, TTooltip]
  icon:
    used_by: [TAlert, TBadge, TButton, TConfirmDialog, TEmptyState, TFileUpload, TStat, TTag]
    note: >-
      A slot named `icon` takes rendered content — put a `TIcon` (or any
      element) inside it. None of these components has an `icon` PROP; the slot
      is the entire interface, so `<TButton icon="plus">` is not a thing. The one
      prop-shaped icon in the library is `TNavMenuItem.icon`, typed `TIconInput`;
      see shared_props → icon.
  prefix:
    used_by: [TCombobox, TDatePicker, TInput, TLanguageSelect]
  suffix:
    used_by: [TDatePicker, TInput]
  trigger:
    used_by: [TConfirmDialog, TDrawer, TModal, TTooltip]
  content:
    used_by: [TDrawer, TModal, TTooltip]
  header:
    used_by: [TCard, TDrawer, TModal]
  footer:
    used_by: [TCard, TDrawer, TModal]

value_formats:
  date: "YYYY-MM-DD"

dataviz_contract:
  components: [TChart, TSparkline, TDonutChart]
  engine: "Pure geometry helpers live in @treeui/utils (niceScale, linearScale, buildLinePath, buildAreaPath, donutSegments) — framework-agnostic and reusable from React."
  series_shape:
    used_by: [TChart]
    type: "{ name: string; data: number[]; color?: string }"
    note: "Each series' `data` is aligned index-for-index to the `labels` array."
  segment_shape:
    used_by: [TDonutChart]
    type: "{ label: string; value: number; color?: string }"
    note: "Non-positive `value`s are omitted; fractions are computed from the sum of the rest."
  palette:
    tokens: "--tree-color-chart-1 … --tree-color-chart-8"
    rule: "Series/segments auto-assign these in order (cycling after 8). A per-series `color` overrides. Never map color to rank — keep a series' color stable when the set changes."
  value_format:
    prop: valueFormat
    type: "(value: number) => string"
    used_by: [TChart, TDonutChart]
    default: "Locale-grouped integers for |value| >= 1000, raw otherwise."
  accessibility:
    - "TChart renders a visually-hidden <table> mirror of the series data; the SVG is aria-hidden."
    - "TSparkline and TDonutChart expose role=img with an aria-label (auto-generated, overridable)."
    - "A legend is shown by default for >= 2 series/segments so identity never relies on color alone."
    - "Entrance animation is gated behind prefers-reduced-motion."

overlay_contract:
  props:
    - open
    - defaultOpen
  events:
    - update:open
    - open-change
  rule: "controlled and uncontrolled modes should behave the same from the outside"

form_contract:
  props:
    - modelValue
  events:
    - update:modelValue

responsive_contract:
  props:
    - mobile
    - breakpoint
  used_by:
    - TAppShell
  rule: "`mobile` is an optional boolean override; when left undefined the component derives the mode from `breakpoint` (a CSS max-width). An explicit `mobile` value always wins over the breakpoint."

accessibility_baseline:
  focus_visible: true
  keyboard_support: true
  semantic_markup: true
  disabled_state_clarity: true
  loading_state_clarity: true
  screen_reader_labels_when_needed: true

notes:
  - "Use T<Name> in examples, docs, and code. There are no Tree<Name> aliases."
  - "Do not introduce Vue-specific naming into tokens or design contracts."
